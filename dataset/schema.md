# BCS Question Bank — Schema (10th–50th)

> **Source of truth:** `data/processed/json/*_bcs.json` (41 files, `5,350` questions) + merged `bcs_preliminary_question_bank.json` — generated by `pipeline/build.py:8` and validated by `pipeline/validate.py:23` against `topics_taxonomy.md:5`. See `.agent/AGENTS.md:38` for JSON shape.

**Version:** `1.0` • **Updated:** `2026-08-31` (17th BCS corrected 70→100) • **Encoding:** UTF-8, Bangla Unicode preserved (no mojibake) • **Total:** `41 exams`, `5,350 questions`, `825 images` (19 MB)

---

## 1. File Layout

```
data/processed/json/10th_bcs.json … 50th_bcs.json   # per-exam canonical DB (100/200 per era, 19th:50)
bcs_preliminary_question_bank.json                  # merged DB (root) — canonical for bcs preliminary question bank
data/raw/html/17th_bcs.html                         # source HTML scraped from https://uttoron.academy/QuestionBank/QuestionPreview/17th-bcs
topics_taxonomy.md                     # ground truth 10 subjects (IDs 1..10)
images/<exam_slug>/q<N>_img<K>.(jpg|png)             # 825 files, exam-scoped, per-question monotonic index
```

Pipeline entry: `pipeline/main.py:38` `TAXONOMY_PATH = ROOT / "topics_taxonomy.md"` • `scrape_questions.py:16` `LINKS_FILE = ROOT / "instructions/links_to_scrape.md"`

---

## 2. Taxonomy (Ground Truth — 10 subjects)

`topics_taxonomy.md:5` — fixed IDs `1..10`, never renamed.

| subject_id | subject_en                                   | subject_bn                          |
| ---------: | -------------------------------------------- | ----------------------------------- |
|          1 | Bangla Language & Literature                 | বাংলা ভাষা ও সাহিত্য                |
|          2 | English Language & Literature                | English Language & Literature       |
|          3 | Bangladesh Affairs                           | বাংলাদেশ বিষয়াবলি                  |
|          4 | International Affairs                        | আন্তর্জাতিক বিষয়াবলি               |
|          5 | Geography, Environment & Disaster Management | ভূগোল, পরিবেশ ও দুর্যোগ ব্যবস্থাপনা |
|          6 | General Science                              | সাধারণ বিজ্ঞান                      |
|          7 | Computer & Information Technology            | কম্পিউটার ও তথ্যপ্রযুক্তি           |
|          8 | Mathematical Reasoning                       | গাণিতিক যুক্তি                      |
|          9 | Mental Ability                               | মানসিক দক্ষতা                       |
|         10 | Ethics, Values & Good Governance             | নৈতিকতা, মূল্যবোধ ও সুশাসন          |

Distribution (5,350 rows, post 17th fix): `1:1008, 2:977, 3:837, 4:754, 8:578, 6:524, 9:215, 7:202, 5:165, 10:126` (approx; run `python -m pipeline.merge` to recompute).

---

## 3. Per-Exam JSON Shape

`pipeline/build.py:28` `build_exam_json()` • Top-level per `data/processed/json/17th_bcs.json:1`:

```json
{
  "exam": {
    "title": "17th BCS",
    "date": "1995-01-01",
    "total_marks": 100,
    "set_code": "পদ্ম",
    "total_questions": 100
  },
  "taxonomy": [
    {
      "id": 1,
      "subject_en": "Bangla Language & Literature",
      "subject_bn": "বাংলা ভাষা ও সাহিত্য"
    },
    {
      "id": 10,
      "subject_en": "Ethics, Values & Good Governance",
      "subject_bn": "নৈতিকতা, মূল্যবোধ ও সুশাসন"
    }
  ],
  "questions": [
    /* 100 items, sorted by question_number */
  ]
}
```

| Field                  | Type                  | Constraint                        |
| ---------------------- | --------------------- | --------------------------------- |
| `exam.title`           | `string`              | e.g. `"50th BCS"`                 |
| `exam.date`            | `string (YYYY-MM-DD)` | e.g. `"2026-01-30"`               |
| `exam.total_marks`     | `int`                 | `50,100,200` per era              |
| `exam.set_code`        | `string`              | Bengali, e.g. `"ধরলা"` / `"পদ্ম"` |
| `exam.total_questions` | `int`                 | `== len(questions)`               |
| `taxonomy`             | `array`               | 10 entries, `id 1..10`            |
| `questions`            | `array`               | sorted `question_number 1..N`     |

---

## 4. Question Object (per row)

`pipeline/build.py:8` `QUESTION_KEYS` — order is canonical. New field `exam_slug` added `2026-08-31`.

```json
{
  "question_number": 1,
  "subject_id": 9,
  "question": "প্রশ্নের plain text (UTF-8 Bangla+English+math); empty ONLY if question_image_paths non-empty",
  "option_a": "A option text",
  "option_b": "B option text",
  "option_c": "C option text",
  "option_d": "D option text; may be '' only if option was image (now none after fixes, keep nullable handling)",
  "correct_answer": "B",
  "solve_note": "ব্যাখ্যার plain text; '' allowed only if solve_note_image_paths non-empty",
  "question_image_paths": ["images/17th_bcs/q9_img1.png"],
  "solve_note_image_paths": [],
  "has_question_image": 0,
  "has_solve_note_image": 0,
  "has_image": 0,
  "subject_en": "Mental Ability",
  "subject_bn": "মানসিক দক্ষতা",
  "exam_slug": "17th_bcs"
}
```

### Field Details

| Field                    | Type            | Rules / Notes                                                                                                        |
| ------------------------ | --------------- | -------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | --- | ------------------------------------------------------------------------------------------------------------- |
| `question_number`        | `int`           | `1..total_questions`, unique per `exam_slug`                                                                         |
| `subject_id`             | `int 1..10`     | FK → taxonomy, must match `subject_en/bn`                                                                            |
| `question`               | `text`          | UTF-8; `""` allowed only if `question_image_paths != []` (image-only). 0 such rows after curated batch, keep support |
| `option_a..d`            | `text`          | `option_d` may be `""` only if option was image (rare, flag)                                                         |
| `correct_answer`         | `char(1) or ""` | `"A"                                                                                                                 | "B"                                            | "C" | "D"`or`""`= defective source (8 rows intentionally blank, e.g.`25th q65`). Keep nullable/blank, do not invent |
| `solve_note`             | `text`          | `""` allowed only if `solve_note_image_paths != []`                                                                  |
| `question_image_paths`   | `text[]`        | local relative paths `images/<exam_slug>/q<N>\_img<K>.(jpg                                                           | png)`; question scope first                    |
| `solve_note_image_paths` | `text[]`        | same convention, solve scope                                                                                         |
| `has_question_image`     | `0              | 1`                                                                                                                   | `= (question_image_paths != [])`               |
| `has_solve_note_image`   | `0              | 1`                                                                                                                   | `= (solve_note_image_paths != [])`             |
| `has_image`              | `0              | 1`                                                                                                                   | `= has_question_image OR has_solve_note_image` |
| `subject_en/bn`          | `text`          | denormalized, must match `subject_id` per taxonomy                                                                   |
| `exam_slug`              | `string`        | canonical file stem e.g. `50th_bcs`, `17th_bcs`; must match `^\d+(st                                                 | nd                                             | rd  | th)\_bcs$`and file name; added for per-row filtering.`pipeline/build.py:31` `q["exam_slug"]=exam.stem`        |

**Image naming:** `q<N>_img<K>` per-question monotonic: question images first (`q173_img1`), then solve (`q173_img2`), never reuse index. `pipeline/images.py:147`.

### Validation Checklist

`pipeline/validate.py:23` `validate_json_file()` — must pass before commit (see `.agent/AGENTS.md:100`):

- JSON parses UTF-8, `total_questions == len(questions)`
- `question_number` int, unique, sequential `1..N` per exam
- No `question_html`, no generic `image_paths`
- `subject_id 1..10`, `subject_en/bn` matches taxonomy
- No mojibake (`à¦, à§, â€, Â, �`) — `pipeline/common.py:6` `MOJIBAKE_MARKERS`
- `solve_note` empty only if `solve_note_image_paths` non-empty; `question` empty only if `question_image_paths` non-empty
- Every image path exists on disk, exam-scoped `images/<exam_slug>/`
- Flags match arrays, `exam_slug` matches file stem and pattern

---

## 5. Merged Database

`bcs_preliminary_question_bank.json:1` + `pipeline/merge.py:24` `merge()`:

```json
{
  "metadata": {
    "title": "BCS Preliminary Question Bank Database 10th to 50th",
    "name": "bcs_preliminary_question_bank",
    "version": "1.0",
    "generated_at": "2026-08-31T05:42:00+00:00",
    "total_exams": 41,
    "total_questions": 5350,
    "source": "data/processed/json/*_bcs.json",
    "taxonomy_source": "topics_taxonomy.md"
  },
  "taxonomy": [
    /* 10 */
  ],
  "exams": [
    {
      "slug": "10th_bcs",
      "title": "10th BCS",
      "date": "1989-01-01",
      "total_marks": 100,
      "set_code": "সুরমা",
      "total_questions": 100,
      "actual_questions": 100
    },
    {
      "slug": "17th_bcs",
      "title": "17th BCS",
      "date": "1995-01-01",
      "total_marks": 100,
      "set_code": "পদ্ম",
      "total_questions": 100,
      "actual_questions": 100
    }
    // ... 41 sorted by numeric exam 10..50
  ],
  "questions": [
    /* 5350 sorted by (exam_num, question_number), each has exam_slug */
  ]
}
```

Per-exam counts: `10th:100, 11st:100, 12nd:100, 13rd:100, 14th:100, 15th:100, 16th:100, 17th:100 (corrected 70→100 2026-08-31), 18th:100, 19th:50, 20th:100, 21st:100, 22nd:100, 23rd:100, 24th:100, 25th:100, 26th:200, 27th:100, 28th:100, 29th:100, 30th:100, 31st:100, 32nd:100, 33rd:100, 34th:100, 35th:200, 36th:200, 37th:200, 38th:200, 39th:100, 40th:200, 41st:200, 42nd:100, 43rd:200, 44th:200, 45th:200, 46th:200, 47th:200, 48th:100, 49th:100, 50th:200`.

Regenerate: `python -m pipeline.merge` (writes `bcs_preliminary_question_bank.json`).

## 9. Constraints & Notes

- Keep `correct_answer` blank/null semantics (8 defective rows). Do not coalesce.
- Images are **paths/URLs**, never base64 in DB. `images/` is source for `storage` upload.
- `exam_slug` is denormalized on `questions` for fast `WHERE exam_slug='17th_bcs'` without join.
- Assume **read-heavy** (filter by exam/subject, search, paginate `?limit=50&offset=0`). Admin writes only.
- Regeneration: fix HTML → `python -m pipeline <html>` → JSON rebuilds; or direct JSON edit for text fixes (do not re-run without re-applying manual patches noted in `instructions/memory.md`).
- See `pipeline/validate.py:23` and `.agent/AGENTS.md:100` for hard checks before any DB import.
